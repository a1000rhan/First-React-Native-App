import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  OrderGrid: {
    display: "flex",
    flexDirection: "row",
    width: "100%",
    flexWrap: "wrap",
  },
});

export default styles;
